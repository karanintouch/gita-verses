{
  "name": "gita-verses",
  "private": true,
  "description": "Static single-page Bhagavad Gita verse browser (no build step). Tests only.",
  "scripts": {
    "test": "node --test tests/",
    "test:data": "node --test tests/data.test.js"
  },
  "devDependencies": {
    "playwright": "^1.56.1"
  }
}
